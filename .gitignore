# Setup script inhe download karta hai - repo me nahi jaane chahiye (bahut bade hain)
app/bin/
app/runtime/

# Remote access key — har machine par apni alag generate hoti hai
app/access-key.txt

# Tunnel config — machine-specific (tunnel id + credentials path)
cloudflare-config.yml

# Full-video cache (internal scratch space)
app/cache/

# Purane installs ki clips (ab system ke Downloads folder mein jaati hain)
app/downloads/

node_modules/
package-lock.json
*.bak_*
.claude/
.DS_Store
